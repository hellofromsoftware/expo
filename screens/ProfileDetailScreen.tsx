import React from 'react';
import { ScrollView, Text } from 'react-native';

export default function ProfileDetailScreen() {
  return (
    <ScrollView>
      <Text style={{ fontSize: 20, fontWeight: 'bold' }}>Profile Details</Text>
      {/* TODO: Render profile detail data */}
    </ScrollView>
  );
}
